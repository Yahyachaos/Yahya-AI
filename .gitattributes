# Large canonical Celine source assets are stored via Git LFS.
# Keep generated/test GLBs outside this path unless they also need persistence.
app/src/main/assets/models/source/*.glb filter=lfs diff=lfs merge=lfs -text
